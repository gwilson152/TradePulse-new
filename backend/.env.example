# Server Configuration
PORT=9000
ENVIRONMENT=development

# Database Configuration
DB_HOST=postgres1.drivenw.local
DB_PORT=5432
DB_NAME=tradepulse
DB_USER=tradepulse
DB_PASSWORD=drv13llc!
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRY=24h

# CORS Configuration
ALLOWED_ORIGINS=https://tradepulse.drivenw.com:4000

# Email Configuration
EMAIL_PROVIDER=smtp
# Options: smtp, graph, gmail

# SMTP Configuration (for EMAIL_PROVIDER=smtp)
SMTP_HOST=drivenw-com.mail.protection.outlook.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=support@drivenw.com

# Microsoft Graph API (for EMAIL_PROVIDER=graph)
GRAPH_TENANT_ID=
GRAPH_CLIENT_ID=
GRAPH_CLIENT_SECRET=
GRAPH_FROM_EMAIL=

# Google API (for EMAIL_PROVIDER=gmail)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_FROM_EMAIL=

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760

# Magic Link Configuration
MAGIC_LINK_EXPIRY=15m
MAGIC_LINK_BASE_URL=https://tradepulse.drivenw.com
